只是用arraylist的add方法添加对象是，如果是重复的对象则会覆盖前一个值，需要new一个新的对象存储，仅仅改变旧的对象的属性值添加
对象是不行的，因为对象的是没有变化的。


servlet处理参数
**************************************************************
在一个servlet接受一个参数时，需要对这个参数的合法性进行判断。特别是在针对这个参数有复杂的逻辑过程时，一定得分析逻辑判断参数
的合法性。这里的合法性是指参数的范围类型。不仅在servlet中需要注意这个问题，在任何复杂的程序中都需要留这份心。